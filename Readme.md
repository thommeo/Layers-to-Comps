Layers to Comps
================

This is script for Photoshop to automatically create Layer Comps from selected Layers. Every Layer Comp will represent every selected layer.

Usage: select the layers and run the script.
